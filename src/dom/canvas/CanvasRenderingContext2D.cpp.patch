// GLINT FARBLING PATCH
#include "mozilla/Preferences.h"
#include "mozilla/glint/GlintFarbling.h"

{
  static bool canvasNoiseEnabled = false;
  static bool initialized = false;
  if (!initialized) {
    Preferences::AddBoolVarCache(&canvasNoiseEnabled, "privacy.farbling.canvas.noise", false);
    initialized = true;
  }

  if (canvasNoiseEnabled && mCanvasElement) {
    nsAutoCString host;
    nsresult rv = GetCurrentHost(host);
    if (NS_SUCCEEDED(rv) && !host.IsEmpty()) {
      mozilla::glint::GlintFarbling::AddCanvasNoise(aData, aWidth, aHeight);
    }
  }
}
// END GLINT FARBLING PATCH
